import React from 'react'

const ClientLayou = ({ children }) => {
    return (
        <div>
            <main className='min-h-screen'>{children}</main>
        </div>
    )
}

export default ClientLayou
